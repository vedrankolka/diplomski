version: '3.3'

services:

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: wpdb
      MYSQL_USER: gudi
      MYSQL_PASSWORD: password
      MYSQL_RANDOM_ROOT_PASSWORD: '1'
    volumes:
      - db:/var/lib/mysql

  wordpress:
    image: wordpress
    hostname: wordpress
    restart: always
    ports:
      - 8080:80
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: gudi
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: wpdb
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_HOME', 'http://example.com');
        define('WP_SITEURL', 'http://example.com');
    depends_on:
      - db
    volumes:
      - wordpress:/var/www/html

  # scanning attack on the server
  droopescan:
    image: droope/droopescan:latest
    entrypoint: "droopescan"
    # TODO dodat sleep $((1 + $RANDOM % 5)) &&
    command: "scan wordpress -u http://wordpress:80"
    depends_on:
      - wordpress
    volumes:
      - droopescan:/home/droopescan

  # locust agent
  locust:
    image: locustio/locust
    volumes:
      - ./:/mnt/locust
    depends_on:
      - wordpress
    command: -f /mnt/locust/locustfile.py -u 5 -r 1 -H http://wordpress:80 --headless

volumes:
  wordpress:
  db:
  droopescan:
